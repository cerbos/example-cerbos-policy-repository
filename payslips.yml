apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: 'default'
  resource: 'payslips'
  rules:
    - actions: ['*']
      effect: EFFECT_ALLOW
      roles:
        - ORG_ADMIN

    - actions: ['*']
      effect: EFFECT_ALLOW
      roles:
        - EMPLOYEE
